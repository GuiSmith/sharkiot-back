const options = {
    origin: 'https://guismith.github.io/sharkiot',  // Domínio permitido
    methods: ['GET', 'POST', 'PATCH'],  // Métodos permitidos
    // allowedHeaders: ['Content-Type', 'Authorization'],  // Cabeçalhos permitidos
};

export default { options };